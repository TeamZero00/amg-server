import express from "express";
import { Request, Response } from "express";

import { BettingController } from "../controller/db/betting";

const router = express.Router();

router.post("/bet_heistory/:address", );

export default router;
